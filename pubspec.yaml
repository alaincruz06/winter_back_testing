name: winter_back_testing
description: A command-line application to test the winter backend package.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: 3.5.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  winter: ^0.0.7

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
